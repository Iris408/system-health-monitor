# System Health Monitor 

A Python-based system monitoring application that tracks CPU, memory, disk usage, and uptime with threshold-based alerts, logging, Docker support, and a FastAPI dashboard.

# システムヘルスモニター 

Pythonベースのシステム監視アプリケーション。CPU、メモリ、ディスク使用量、稼働時間を追跡し、しきい値に基づくアラート、ログ記録、Dockerサポート、FastAPIダッシュボードを提供します。

## Screenshot

<img src="./screenshots/system-health-monitor.png" width="500"/>


## Features | 機能 

| EN | JP |
| --- | --- |
| CPU, memory, disk, and uptime monitoring | CPU、メモリ、ディスク、稼働時間の監視 |
| Warning and critical threshold detection | 警告および重大なしきい値の検出 |
| Slack alert integration | Slackアラートとの連携 |
| Email alert integration | メールアラートとの連携 |
| Logging support | ログ記録のサポート | 
| Docker container support | Dockerコンテナのサポート |
| FastAPI web dashboard | FastAPIウェブダッシュボード |
| `/health` JSON endpoint | `/health` JSONエンドポイント |
| Configurable thresholds using environment variables | 環境変数によるしきい値の設定 |
| EN/JP code comments for learning and review | 学習と復習のための英語/日本語コードコメント |

---

## Tech Stack | 技術スタック 

- Python
- FastAPI
- Docker
- Docker Compose
- psutil
- colorama
- Slack Webhooks
- SMTP Email
- Git/GitHub

---

## Installation

Clone the repository:
```bash
git clone https://github.com/Iris408/system-health-monitor.git
cd system-health-monitor
```
Install dependencies:
```bash
pip install -r requirements.txt
```
Run the monitor:
```bash
python3 main.py
```

---

## Docker Usage

This project can run inside a Docker container using Docker compose.

Build and run the container
```bash
docker compose up --build
```
Run in the background:
```bash
docker compose up -d
```
View logs:
```bash
docker compose logs
```
Stop the container:
```bash
docker compose down
```

---

## Environment Variables

This project uses environment variables for alerting and threshold configuration.

Create a `/.env` file in the project root.

```env
SLACK_WEBHOOK_URL=your_slack_webhook_url_here
CPU_WARNING_THRESHOLD=70
CPU_CRITICAL_THRESHOLD=90
MEMORY_WARNING_THRESHOLD=70
MEMORY_CRITICAL_THRESHOLD=90
DISK_WARNING_THRESHOLD=70
DISK_CRITICAL_THRESHOLD=90
```

### Sample Log Output
A sample monitoring log is accessible here: 
```text
examples/health_log.txt
```
This shows example monitoring records automatically generated by the application.

--- 

# Future Improvements
- Add automated tests
- Add GitHub Actions CI workflow
- Improve dashboard UI
- Add historical monitoring charts
- Add log filtering
- Add deployment documentation
